# Agent instructions

## Before every git push
1. Update README.md if the changes add, remove, or modify any user-facing feature
2. Update CHANGELOG.md under `[Unreleased]` with all changes (Added / Changed / Fixed sections)
3. Stage all files, then commit and push
